[project]
name = "reclaim-era"
version = "1.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "flask==3.0.3",
  "flask-cors==4.0.1",
  "razorpay==1.4.2",
  "psycopg[binary]>=3.2,<4",
  "python-dotenv==1.0.1",
]

[tool.vercel]
entrypoint = "app:app"
