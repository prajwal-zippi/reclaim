/* ============================================================
   Backend location for the admin CMS.
   - Leave "" if the backend runs on the SAME domain as the site
     (e.g. everything deployed together on Vercel).
   - Otherwise set the deployed backend URL, e.g.
     window.RE_API_BASE = "https://reclaim-xxxx.vercel.app";
   On localhost the site always talks to http://localhost:5001.
   ============================================================ */
window.RE_API_BASE = "";
